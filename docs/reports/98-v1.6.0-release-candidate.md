# Max & Gym v1.6.0 release candidate

Date: 2026-08-16

## Outcome

The release candidate fixes strict equipment filtering, adds goal-aware session generation, makes saved sessions discoverable and reusable, adds persistent active-workout controls, and expands useful non-cable Glutes selection through curated focus metadata.

## Release identity

- App version: `1.6.0`
- Generator: `deterministic-v6`
- Exercise seed: `fedb-b0eed061e1c8-reviewed-6`
- Database schema: `8` (unchanged)
- Export format: `2` (unchanged)
- Cache version: `5` (unchanged)

No data migration, account requirement, new network origin or trail-related feature is introduced.

## Verification

- TypeScript typecheck: passed
- ESLint with zero warnings: passed
- Unit/component suite: 50 files, 198 tests passed
- GitHub Pages production build and subpath smoke: passed
- Android production web bundle and smoke: passed
- Dependency, architecture, Android release, project, network, license, asset, exercise-asset, language, accessibility and performance audits: passed
- Interactive browser QA: unavailable because no browser session was connected; component, route, production and Android bundle checks cover the release behavior

The local machine has no JDK, so signed APK assembly remains delegated to the trusted GitHub Actions release workflow. The delivery boundary is a pushed release branch and reviewable PR; the author does not self-merge.
